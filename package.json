{
  "name": "figma-browser-exporter",
  "version": "0.1.0",
  "private": true,
  "description": "Visible-browser Figma screen and rendered asset exporter.",
  "scripts": {
    "export": "tsx src/cli.ts",
    "test": "vitest run tests",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "commander": "^14.0.2",
    "playwright": "^1.57.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "engines": {
    "node": ">=20"
  }
}
